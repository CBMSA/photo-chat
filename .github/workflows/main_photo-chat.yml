# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy React app to Azure Static Web App - Photo Chat

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy Photo Chat React App

    steps:
      - name: Checkout GitHub Repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm install
        working-directory: ./web

      - name: Build React App
        run: npm run build
        working-directory: ./web

      - name: Deploy to Azure Static Web App
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }} # Auto-generated by GitHub
          action: "upload"
          app_location: "web"
          output_location: "web/build"
          app_artifact_location: "web/build"


---

🔐 Required Secret

You need to add 1 secret in your GitHub repo:

1. AZURE_STATIC_WEB_APPS_API_TOKEN
Get this from Azure:

Go to Azure Portal → Static Web App → "Deployment Token"

Copy and paste it into GitHub → Settings → Secrets → Actions





---

🚫 Remove

You should delete or rename this file if it exists:

.github/workflows/deploy-java-to-appservice.yml

Because it's still pointing to Maven/JAR logic and will conflict.


---

✅ Want Help?

I can:

Generate the file for you

Zip it into your project

Push sample code to GitHub (if you give me structure)

Generate Firebase .env configs for GitHub Actions


Let me know what you’d like next.

